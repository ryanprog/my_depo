{% extends 'base.html.twig' %}

{% block title %}Conclusion{% endblock %}

{% block body %}
    <h1>Conclusion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conclusion.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ conclusion.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('conclusion_index') }}">back to list</a>

    <a href="{{ path('conclusion_edit', {'id': conclusion.id}) }}">edit</a>

    {{ include('conclusion/_delete_form.html.twig') }}
{% endblock %}
