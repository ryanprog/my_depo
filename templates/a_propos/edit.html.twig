{% extends 'base.html.twig' %}

{% block title %}Edit APropos{% endblock %}

{% block body %}
    <h1>Edit APropos</h1>

    {{ include('a_propos/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('a_propos_index') }}">back to list</a>

    {{ include('a_propos/_delete_form.html.twig') }}
{% endblock %}
