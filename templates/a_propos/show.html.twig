{% extends 'base.html.twig' %}

{% block title %}APropos{% endblock %}

{% block body %}
    <h1>APropos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ a_propo.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ a_propo.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('a_propos_index') }}">back to list</a>

    <a href="{{ path('a_propos_edit', {'id': a_propo.id}) }}">edit</a>

    {{ include('a_propos/_delete_form.html.twig') }}
{% endblock %}
