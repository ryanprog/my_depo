{% extends 'base.html.twig' %}

{% block title %}Quisuisje{% endblock %}

{% block body %}
    <h1>Quisuisje</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quisuisje.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ quisuisje.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quisuisje_index') }}">back to list</a>

    <a href="{{ path('quisuisje_edit', {'id': quisuisje.id}) }}">edit</a>

    {{ include('quisuisje/_delete_form.html.twig') }}
{% endblock %}
